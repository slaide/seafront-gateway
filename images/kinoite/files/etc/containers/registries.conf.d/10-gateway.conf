# Trust the gateway's registry. It serves plain HTTP on the isolated backbone
# (no internet, no CA) so it must be marked insecure. This is safe: the backbone
# is a physically isolated switch.
[[registry]]
location = "192.168.50.1:5000"
insecure = true
