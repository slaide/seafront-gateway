# Trust the gateway's registry. It serves plain HTTP on the fleet LAN (no CA) so it
# must be marked insecure. Acceptable on the trusted lab network the fleet runs on.
[[registry]]
location = "10.10.0.69:5000"
insecure = true
