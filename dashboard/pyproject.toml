[project]
name = "microscope-gateway-dashboard"
version = "0.1.0"
description = "Central landing page for the microscope LAN; shows status and links to each microscope."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "httpx>=0.27",
]
